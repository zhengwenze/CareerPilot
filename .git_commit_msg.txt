feat: 匹配报告改为强制AI语义主路径，规则引擎仅作AI输入

核心改动：
- match_ai.py: 统一到现有Minimax JSON client，定义strict JSON报告schema，强制校验完整报告结构
- match_report.py: 将匹配报告生成改为AI强制主路径；规则引擎只作为AI输入，不再作为成功fallback
- config.py: 把匹配AI默认配置切到minimax
- .env.example: 同步默认环境模板，明确匹配报告依赖Minimax
- README.md: 补充后端启动时匹配AI的配置说明

测试覆盖：
- test_jobs_match_flow.py: 新增强制AI语义测试：AI成功、缺key失败、返回非法结构失败
- test_resume_optimizer_flow.py: 修复下游优化闭环测试，使其适配新的强制AI匹配报告

前端改动：
- jobs.ts: 增加单报告详情查询，并收敛scorecard_json类型
- jobs/page.tsx: 完整重写岗位匹配页：极简输入、一键编排流程、默认最新报告展示、历史折叠、自动进简历优化

验证：待执行pytest + lint
