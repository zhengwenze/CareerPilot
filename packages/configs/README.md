# Configs

`packages/configs/` stores shared frontend-oriented config building blocks.

Current scope:

- TypeScript base config
- ESLint base config for Next.js code

These files are intentionally introduced before full workspace wiring so future extraction work has a clear home.
