# 3.16 开发记录

> 记录时间：2026-03-16  
> 开发者：zhengwenze  
> 项目：CareerPilot（职点迷津）

---

## 📊 总览

3 月 16 日是 CareerPilot 项目的关键突破日，完成了岗位匹配模块的重设计与落地、JD 解析器的独立开发、简历解析流程的完善，以及项目文档体系的规范化。全天共提交 **4 次**，涉及岗位匹配、JD 解析、简历解析、前端优化等多个模块。

### 提交统计

- **总提交数**：4 次
- **代码行数**：+3,052 行新增，-506 行删除
- **涉及文件**：39 个
- **主要分支**：`master`

---

## 🎯 核心进展

### 1. 岗位匹配模块重设计与落地（09:00 - 14:00）

#### 09:00 - 数据层重构

```
commit: 8761063
feat: 添加JD解析器和简历解析优化

commit: be914f6
feat: 优化岗位匹配模块和JD解析功能
```

**新增文件**：

- `apps/api/alembic/versions/20260316_0005_expand_job_matching_workflow.py` - 数据库迁移脚本
- `apps/api/app/models/job_parse_job.py` - 岗位解析任务模型
- `apps/api/app/models/job_readiness_event.py` - 岗位准备度事件模型
- `apps/api/app/services/match_support.py` - 匹配支持服务
- `scripts/JD_parser/岗位匹配模块设计方案.md` - 重设计方案文档

**更新文件**：

- `apps/api/app/models/job_description.py` - 扩展岗位模型
- `apps/api/app/models/match_report.py` - 升级匹配报告模型
- `apps/api/app/schemas/job.py` - 新增岗位 Schema
- `apps/api/app/schemas/match_report.py` - 扩展匹配报告 Schema

**核心功能**：

- ✅ 岗位解析任务模型（异步任务化）
- ✅ 岗位准备度事件模型（状态流转追踪）
- ✅ 匹配支持服务（规则对齐、证据映射）
- ✅ 数据库表结构扩展

#### 10:00 - 服务层重构

**更新服务**：

- `apps/api/app/services/job.py` - 岗位服务重构（+466 行）
- `apps/api/app/services/job_parser.py` - JD 解析器优化（+57 行）
- `apps/api/app/services/match_engine.py` - 匹配引擎重构（+72 行）
- `apps/api/app/services/match_report.py` - 匹配报告服务重构（+457 行）

**核心改进**：

- **规则对齐层**：技能、关键词、学历、年限、城市、职责语义簇
- **证据映射层**：JD 要求映射到简历具体字段和文本证据
- **AI 语义补偿层**：补充弱召回项、重写建议文案、生成面试题方向
- **行动生成层**：把差距转成用户能执行的动作

#### 11:00 - API 层完善

**更新路由**：

- `apps/api/app/api/routes/jobs.py` - 岗位路由扩展（+137 行）
- `apps/api/app/api/routes/match_reports.py` - 匹配报告路由优化（+46 行）
- `apps/api/app/api/routes/resumes.py` - 简历路由优化（+79 行）

**新增接口**：

- `POST /jobs` - 创建目标岗位（异步解析）
- `GET /jobs` - 支持多条件过滤（keyword、parse_status、status_stage、stale、priority）
- `GET /jobs/{job_id}` - 返回岗位驾驶舱聚合视图
- `POST /jobs/{job_id}/parse` - 重跑岗位结构化
- `POST /jobs/{job_id}/match-reports` - 生成匹配快照
- `GET /jobs/{job_id}/match-reports` - 返回历史快照列表
- `GET /match-reports/{report_id}` - 返回完整匹配报告

#### 12:00 - 配置与测试

**更新配置**：

- `apps/api/app/core/config.py` - 新增 JD 解析器配置（+38 行）

**更新测试**：

- `apps/api/tests/test_config.py` - 配置测试（+17 行）
- `apps/api/tests/test_jobs.py` - 岗位测试（+37 行）
- `apps/api/tests/test_match_reports.py` - 匹配报告测试（+39 行）
- `apps/api/tests/test_resume.py` - 简历测试（+123 行）
- `apps/api/tests/test_resume_parser.py` - 解析器测试（+32 行）

---

### 2. JD 解析器独立开发（14:00 - 15:00）

#### 14:00 - JD 解析器脚手架

**新增文件**：

- `scripts/JD_parser/岗位匹配模块设计方案.md` - 完整设计方案（184 行）
- `scripts/JD_parser/岗位匹配模块开发清单.md` - 开发任务清单（119 行）

**设计方案核心**：

- **模块定位**：闭环中枢（连接简历、优化、面试模块）
- **岗位来源**：手动录入为主
- **输出资产**：
  - `岗位画像`
  - `简历定制任务包`
  - `模拟面试训练蓝图`

**外部调研借鉴**：

- Teal Job Matcher：岗位匹配驱动简历裁剪
- Jobscan：硬技能、学历、职称、软技能、关键词
- Resume-Matcher：master resume -> job-specific tailoring

#### 14:30 - 状态机设计

**岗位解析状态**：`pending -> processing -> success|failed`

**匹配快照状态**：`pending -> processing -> success|failed`

**岗位准备度状态**：`draft -> analyzed -> matched -> tailoring_needed -> interview_ready -> training_in_progress -> ready_to_apply`

---

### 3. 简历解析流程完善（15:00 - 16:00）

#### 15:00 - 前端优化

**更新文件**：

- `apps/api/app/services/resume_parser.py` - 解析器优化（+46 行）
- `apps/api/app/services/resume.py` - 简历服务优化（+51 行）

**核心改进**：

- 简历上传优化
- PDF 文本抽取改进
- 结构化数据展示优化
- 人工校正功能完善

#### 15:30 - 前端 UI 优化

**更新页面**：

- `apps/web/src/app/(dashboard)/dashboard/resume/page.tsx` - 简历中心页
- `apps/web/src/components/resume/resume-detail-panel.tsx` - 简历详情面板（-17 行）

**删除文案**：

- "解析进行中" 相关说明
- "可直接人工校正" 相关说明
- "状态更新：已经重新触发解析任务" 提示

**原因**：保持界面简洁，减少解释性文案，直接展示功能

---

### 4. 前端岗位匹配界面（16:00 - 18:00）

#### 16:00 - 页面重构

**更新页面**：

- `apps/web/src/app/(dashboard)/dashboard/jobs/page.tsx` - 岗位管理页（+153 行）
- `apps/web/src/app/(dashboard)/dashboard/interviews/page.tsx` - 面试页（+21 行）

**更新组件**：

- `apps/web/src/components/jobs/job-form-card.tsx` - 岗位表单卡片（+176 行）
- `apps/web/src/components/jobs/job-list.tsx` - 岗位列表（+52 行）
- `apps/web/src/components/jobs/match-report-panel.tsx` - 匹配报告面板（+256 行）

**新增 API 模块**：

- `apps/web/src/lib/api/modules/jobs.ts` - 岗位 API（+111 行）

#### 17:00 - 文档更新

**更新文档**：

- `README.md` - 项目文档更新（+178 行）
- `AGENTS.md` - 开发规范更新（+52 行）
- `scripts/README.md` - scripts 目录说明（+109 行）

**删除文件**：

- `apps/api/README.md` - 重复文档
- `apps/web/README.md` - 重复文档

**原因**：统一文档管理，避免重复

---

## 📁 项目结构

```
career-pilot/
├── apps/
│   ├── api/                    # FastAPI 后端
│   │   ├── alembic/
│   │   │   └── versions/
│   │   │       └── 20260316_0005_expand_job_matching_workflow.py  # 新增
│   │   ├── app/
│   │   │   ├── api/
│   │   │   │   ├── routes/
│   │   │   │   │   ├── jobs.py              # 新增/更新
│   │   │   │   │   ├── match_reports.py     # 新增/更新
│   │   │   │   │   └── resumes.py           # 更新
│   │   │   │   └── deps.py
│   │   │   ├── core/
│   │   │   │   ├── config.py                # 更新
│   │   │   │   ├── errors.py
│   │   │   │   ├── logging.py
│   │   │   │   └── responses.py
│   │   │   ├── db/
│   │   │   │   ├── base.py
│   │   │   │   └── session.py
│   │   │   ├── models/
│   │   │   │   ├── __init__.py              # 更新
│   │   │   │   ├── job_description.py       # 更新
│   │   │   │   ├── job_parse_job.py         # 新增
│   │   │   │   ├── job_readiness_event.py   # 新增
│   │   │   │   └── match_report.py          # 更新
│   │   │   ├── schemas/
│   │   │   │   ├── job.py                   # 新增
│   │   │   │   └── match_report.py          # 更新
│   │   │   ├── services/
│   │   │   │   ├── job.py                   # 更新
│   │   │   │   ├── job_parser.py            # 更新
│   │   │   │   ├── match_engine.py          # 更新
│   │   │   │   ├── match_report.py          # 更新
│   │   │   │   ├── match_support.py         # 新增
│   │   │   │   ├── resume.py                # 更新
│   │   │   │   ├── resume_parser.py         # 更新
│   │   │   │   └── storage.py
│   │   │   ├── main.py
│   │   │   └── __init__.py
│   │   ├── tests/
│   │   │   ├── test_config.py               # 更新
│   │   │   ├── test_jobs.py                 # 更新
│   │   │   ├── test_match_reports.py        # 更新
│   │   │   ├── test_models_metadata.py      # 更新
│   │   │   ├── test_resume.py               # 更新
│   │   │   └── test_resume_parser.py        # 更新
│   │   ├── .env.example
│   │   ├── alembic.ini
│   │   ├── pyproject.toml
│   │   └── uv.lock
│   └── web/                     # Next.js 前端
│       ├── src/
│       │   ├── app/
│       │   │   ├── (dashboard)/
│       │   │   │   ├── dashboard/
│       │   │   │   │   ├── jobs/            # 更新
│       │   │   │   │   ├── interviews/      # 更新
│       │   │   │   │   └── resume/          # 更新
│       │   │   │   ├── error.tsx
│       │   │   │   ├── layout.tsx
│       │   │   │   └── loading.tsx
│       │   │   ├── login/
│       │   │   │   └── page.tsx
│       │   │   ├── register/
│       │   │   │   └── page.tsx
│       │   │   ├── favicon.ico
│       │   │   ├── globals.css
│       │   │   ├── layout.tsx
│       │   │   └── page.tsx
│       │   ├── components/
│       │   │   ├── jobs/
│       │   │   │   ├── job-form-card.tsx    # 更新
│       │   │   │   ├── job-list.tsx         # 更新
│       │   │   │   └── match-report-panel.tsx  # 更新
│       │   │   ├── resume/
│       │   │   │   └── resume-detail-panel.tsx  # 更新
│       │   │   └── ui/
│       │   │       ├── alert.tsx
│       │   │       ├── badge.tsx
│       │   │       ├── button.tsx
│       │   │       ├── card.tsx
│       │   │       ├── input.tsx
│       │   │       ├── label.tsx
│       │   │       └── textarea.tsx
│       │   ├── lib/
│       │   │   ├── api/
│       │   │   │   ├── modules/
│       │   │   │   │   └── jobs.ts          # 新增
│       │   │   │   ├── client.ts
│       │   │   │   └── contracts.ts
│       │   │   ├── auth-storage.ts
│       │   │   └── utils.ts
│       ├── .env.example
│       ├── .gitignore
│       ├── components.json
│       ├── eslint.config.mjs
│       ├── next.config.ts
│       ├── package.json
│       ├── package-lock.json
│       ├── postcss.config.mjs
│       └── tsconfig.json
├── scripts/
│   ├── README.md              # 更新
│   └── JD_parser/             # 新增
│       ├── 岗位匹配模块设计方案.md
│       └── 岗位匹配模块开发清单.md
├── .gitignore
├── .python-version
├── 3.16开发记录.md            # 新增
├── AGENTS.md                  # 更新
├── docker-compose.middleware.yml
├── LICENSE
└── README.md                  # 更新
```

---

## 🔑 核心技术栈

### 后端

- **框架**：FastAPI 0.115.0
- **数据库**：PostgreSQL + Alembic
- **认证**：JWT (PyJWT)
- **验证**：Pydantic 2.10.0
- **配置**：pydantic-settings 2.6.1
- **对象存储**：MinIO
- **PDF 处理**：PyMuPDF、pypdf
- **测试**：pytest 8.3.3

### 前端

- **框架**：Next.js 16
- **UI 库**：shadcn/ui + Tailwind CSS
- **状态管理**：React Context
- **路由守卫**：自定义高阶组件
- **API 客户端**：自定义 fetch 封装

---

## 📈 开发成果

### 代码统计

- **新增文件**：7 个
- **更新文件**：32 个
- **删除文件**：2 个
- **代码行数**：+3,052 / -506
- **测试文件**：5 个
- **API 接口**：7 个
- **数据库迁移**：1 个
- **文档**：3 篇

### 功能完成度

- ✅ 岗位解析任务模型（异步任务化）
- ✅ 岗位准备度事件模型（状态流转追踪）
- ✅ 匹配支持服务（规则对齐、证据映射）
- ✅ JD 解析器独立开发
- ✅ 简历解析流程优化
- ✅ 岗位匹配模块重设计
- ✅ 前端岗位管理页面重构
- ✅ 统一响应格式
- ✅ 权限控制
- ✅ 错误处理

---

## 🎨 关键特性

### 1. 岗位匹配模块重设计

**三层架构**：

- **数据层**：岗位模型、匹配报告模型、解析任务模型
- **服务层**：岗位服务、匹配服务、匹配支持服务
- **API 层**：RESTful 接口、统一响应格式

**四层匹配引擎**：

- **规则对齐层**：技能、关键词、学历、年限、城市、职责语义簇
- **证据映射层**：JD 要求映射到简历具体字段和文本证据
- **AI 语义补偿层**：补充弱召回项、重写建议文案、生成面试题方向
- **行动生成层**：把差距转成用户能执行的动作

**输出资产**：

- `岗位画像`：must_have、nice_to_have、responsibility_clusters、experience_constraints、domain_context
- `简历定制任务包`：tailoring_plan_json
- `模拟面试训练蓝图`：interview_blueprint_json

### 2. JD 解析器独立开发

**独立脚手架**：

- `scripts/JD_parser/` - 独立 JD 解析模块
- `岗位匹配模块设计方案.md` - 完整设计方案
- `岗位匹配模块开发清单.md` - 开发任务清单

**设计原则**：

- 模块定位：闭环中枢
- 岗位来源：手动录入为主
- 输出资产：岗位画像、简历定制任务包、模拟面试训练蓝图

### 3. 简历解析流程优化

**前端优化**：

- 删除冗余文案（解析进行中、可直接人工校正、状态更新提示）
- 保持界面简洁，直接展示功能
- 减少解释性文案，提升用户体验

**服务优化**：

- 简历上传优化
- PDF 文本抽取改进
- 结构化数据展示优化
- 人工校正功能完善

### 4. 状态机设计

**岗位解析状态**：`pending -> processing -> success|failed`

**匹配快照状态**：`pending -> processing -> success|failed`

**岗位准备度状态**：`draft -> analyzed -> matched -> tailoring_needed -> interview_ready -> training_in_progress -> ready_to_apply`

---

## 📝 经验总结

### 成功实践

1. **模块化设计**：岗位匹配、JD 解析、简历解析模块独立开发
2. **分层架构**：数据层、服务层、API 层清晰分层
3. **状态机驱动**：岗位解析、匹配快照、岗位准备度状态流转清晰
4. **四层匹配引擎**：规则对齐、证据映射、AI 语义补偿、行动生成
5. **输出资产化**：岗位画像、简历定制任务包、模拟面试训练蓝图
6. **文档先行**：设计方案、开发清单先于代码
7. **测试驱动**：先写测试用例，再实现功能
8. **界面简化**：删除冗余文案，保持界面简洁

### 待优化点

1. 岗位匹配算法可以更精细化
2. 小程序功能可以进一步扩展
3. scripts 目录可以添加更多工具脚本
4. 测试覆盖率可以更高
5. 性能优化：PDF 提取性能优化

---

## 🚀 下一步计划

1. **岗位匹配优化**：优化匹配算法、增加匹配度评分
2. **小程序开发**：完善小程序功能、对接后端 API
3. **scripts 工具**：添加更多独立测试脚本
4. **单元测试**：提高测试覆盖率
5. **性能优化**：PDF 提取性能优化
6. **简历优化模块**：消费 `tailoring_plan_json`，生成可执行改进建议
7. **模拟面试模块**：消费 `interview_blueprint_json`，开展 AI 面试训练

---

## 📚 相关文档

- [README.md](README.md) - 中文文档
- [3.14开发记录.md](3.14开发记录.md) - 3 月 14 日开发记录
- [3.15开发记录.md](3.15开发记录.md) - 3 月 15 日开发记录
- [3.16开发记录.md](3.16开发记录.md) - 3 月 16 日开发记录
- [scripts/JD_parser/岗位匹配模块设计方案.md](scripts/JD_parser/岗位匹配模块设计方案.md) - 岗位匹配重设计方案
- [scripts/JD_parser/岗位匹配模块开发清单.md](scripts/JD_parser/岗位匹配模块开发清单.md) - 岗位匹配开发清单
